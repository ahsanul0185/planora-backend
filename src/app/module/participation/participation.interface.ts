// Currently blank as we don't have particular complex payloads besides validation inputs.
// Future payload interfaces for participation should be added here.
export interface IParticipationQuery {
    status?: string;
    searchTerm?: string;
}
